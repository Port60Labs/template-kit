{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://developers.port60.co.uk/schemas/template-manifest-v1.json",
  "title": "Port60 template manifest (contract v1)",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "version", "format", "supports"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]{1,48}[a-z0-9]$",
      "description": "The template's identity, lowercase, hyphenated, stable across versions."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(?:-dev\\.\\d+)?$",
      "description": "Semver. Stable versions are immutable once submitted. Owner-only development versions use x.y.z-dev.N, remain non-promotable, and may be replaced before creating a stable release."
    },
    "format": {
      "type": "string",
      "enum": ["port60-liquid@1"],
      "description": "The dialect/contract major this template is written against."
    },
    "label": { "type": "string", "maxLength": 60, "description": "Human-readable name shown in pickers." },
    "description": { "type": "string", "maxLength": 300 },
    "requiresCapabilities": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "giving",
          "appeals",
          "worship",
          "courses",
          "membership",
          "events",
          "articles",
          "services",
          "volunteering",
          "forms",
          "resources",
          "locations",
          "newsletter",
          "i18n",
          "search",
          "media"
        ]
      },
      "uniqueItems": true,
      "default": [],
      "description": "Platform capabilities this design needs in order to work as intended. This is a catalogue matching signal, never an entitlement grant. The validator checks that each declared capability has a corresponding section, page template, island or context surface."
    },
    "suitsProfiles": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["general", "mosque", "church", "appeal-charity", "pta", "community", "training"]
      },
      "uniqueItems": true,
      "default": [],
      "description": "Organisation profiles this design was composed for. An empty list means the template is general purpose. Profiles improve catalogue recommendations but do not hide or unlock features."
    },
    "changelog": {
      "type": "string",
      "maxLength": 300,
      "description": "One human line describing what THIS version changed, shown on the Appearance update nudge."
    },
    "supports": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pages", "sections"],
      "properties": {
        "pages": {
          "type": "array",
          "items": { "type": "string", "enum": ["home", "about"] },
          "minItems": 1,
          "uniqueItems": true,
          "description": "Section based page bodies this template renders from sections/{type}.liquid. Contract v1 supports home and about. This is independent of layout chrome and route-specific pageTemplates."
        },
        "sections": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "uniqueItems": true,
          "description": "Section types this template ships renderers for (sections/{type}.liquid). Unsupported types are omitted at render, never an error."
        },
        "islands": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": [],
          "description": "Islands this template places. Every {% island %} name must be listed here AND exist in the platform island registry."
        },
        "layout": {
          "type": "boolean",
          "default": false,
          "description": "True when the template ships layout.liquid and owns the page chrome (header/nav/footer). The platform still owns <head>, consent, and identity, see the layouts guide."
        },
        "pageTemplates": {
          "type": "array",
          "items": { "type": "string", "enum": ["events", "course", "articles", "article"] },
          "uniqueItems": true,
          "default": [],
          "description": "Route-specific data views rendered from pages/{page}.liquid with documented context. Events and articles own listings; course and article own detail presentation. Event transactions, course enrolment, article engagement, comments, identity and consent stay platform-owned islands. Requires supports.layout; without an entry the platform body renders inside the template chrome."
        },
        "worship": {
          "type": "boolean",
          "default": false,
          "description": "True when the layout renders the `worship` context (the prayer/service-times rail). Declared honesty: the choosers badge worship-enabled tenants toward looks that show their times, and warn on looks that don't. Requires supports.layout; the layout must actually branch on `worship`."
        },
        "heroImagery": {
          "type": "boolean",
          "default": false,
          "description": "True when the homeHero renderer displays the tenant's hero photographs (homeHero.images, legacy imageUrl), always under a TREATMENT built from the template's own palette variables (scrim/tint/blend), never raw. Declared honesty, proven behaviourally at validation: the choosers badge photo-led tenants toward templates that show their images and warn on ones that won't. Requires the homeHero section among supports.sections."
        },
        "behaviors": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["reveal", "counter", "progress", "countdown", "accordion", "carousel", "stickyHeader", "stickyCta", "lightbox", "tabs"]
          },
          "uniqueItems": true,
          "description": "Engine-attached behaviours this template's markup opts into via data-p60-* attributes (see the behaviour catalogue). Templates never ship JavaScript; declaring here is what makes the engine wire the markup, and the validator proves declaration and usage agree in both directions."
        }
      }
    },
    "imagery": {
      "type": "object",
      "additionalProperties": false,
      "description": "The template's OWN image advice, surfaced beside uploads in the charity's editor, a design statement (what shape suits THIS composition), advisory only, never enforced. Entity images (events, courses, articles) are platform-scoped instead: see the contract's imagery.json envelopes.",
      "properties": {
        "hero": {
          "type": "object",
          "additionalProperties": false,
          "required": ["idealAspect", "minWidth"],
          "properties": {
            "idealAspect": {
              "type": "string",
              "pattern": "^\\d+:\\d+$",
              "description": "The aspect this hero composes best with, e.g. \"16:9\"."
            },
            "minWidth": {
              "type": "integer",
              "minimum": 800,
              "description": "Below this pixel width the photo may look soft full-bleed."
            },
            "note": {
              "type": "string",
              "maxLength": 240,
              "description": "One human line of advice, e.g. how panoramas or portraits behave in this hero."
            }
          }
        }
      }
    },
    "fonts": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^https://fonts\\.googleapis\\.com/css2\\?"
      },
      "default": [],
      "description": "Webfont stylesheet URLs the platform loads in <head> (with preconnects, never a CSS @import). Google Fonts css2 URLs only."
    },
    "looks": {
      "type": "array",
      "maxItems": 8,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "values"],
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 40,
            "description": "The look's evocative, author-chosen name, shown as a one-click chip in Appearance."
          },
          "values": {
            "type": "object",
            "minProperties": 1,
            "propertyNames": { "pattern": "^[a-zA-Z][a-zA-Z0-9]{0,39}$" },
            "additionalProperties": { "type": "string" },
            "description": "Knob key → value. Every key must be a declared settings knob; select values must be listed options; font values must be catalogue families."
          }
        }
      },
      "default": [],
      "description": "One-click LOOKS: author-named bundles of knob values (scheme + fonts + width…) applied together. Everything remains individually adjustable afterwards."
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "schema": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["key", "kind", "label"],
            "properties": {
              "key": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9]{0,39}$" },
              "kind": { "type": "string", "enum": ["color", "select", "toggle", "font"] },
              "label": { "type": "string", "maxLength": 60 },
              "default": {},
              "options": { "type": "array", "items": { "type": "string" } },
              "group": {
                "type": "string",
                "enum": ["typography", "looks"],
                "description": "Where the admin surfaces this knob: 'typography' → the Typography section beside the font slots; 'looks' → the Looks section with the one-click bundles (colour schemes and accent dials belong there, and ungrouped color knobs plus a knob keyed 'scheme' default there anyway); other ungrouped knobs render under Template options."
              },
              "weights": {
                "type": "array",
                "items": { "type": "integer", "minimum": 100, "maximum": 1000 },
                "uniqueItems": true,
                "description": "FONT knobs only: the weights this template's typographic system uses for the slot. The host requests these for the tenant's chosen family (clamped to the family's real weights). The tenant picks the FAMILY; weights, sizes and tracking stay template-owned."
              }
            }
          },
          "description": "The template's own theming knobs (colour schemes etc.), rendered as an editor in Appearance/get-started; values land in site-config config.theme."
        }
      }
    }
  }
}
